apiVersion: v1
kind: Service
metadata:
  name: card-game-platform
  labels:
    app: card-game-platform
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: card-game-platform
