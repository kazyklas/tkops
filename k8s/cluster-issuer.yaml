apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: http-01
spec:
  acme:
    email: tomas.klas.41@gmail.com
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: letsencrypt-staging-account-key
    solvers:
      - http01:
          ingress:
            class: nginx
